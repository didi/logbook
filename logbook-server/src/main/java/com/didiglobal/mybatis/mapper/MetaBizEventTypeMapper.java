package com.didiglobal.mybatis.mapper;

import org.apache.ibatis.annotations.Mapper;

/**
 * @author mayingdong
 * @date 2021/11/3
 */
@Mapper
public interface MetaBizEventTypeMapper {
//    @Select("SELECT * FROM biz_event_type WHERE status = 1")
//    List<BizEventType> queryAllValidBizEventConfig();

    // todo 需要一个 insert 接口
}
